{% extends 'base/website.html.twig' %}

{% block main %}
    {% include "forum/nav_forum.html.twig" %}
    <a href="{{path('app_forum_announcements')}}" class="btn btn-primary"><i class="fa-solid fa-arrow-left"></i> Retour</a>

    <div class="container-fluid w-75">
        <div class="card mt-3 p-4 shadow">
            <div class="card-header">
                <h2>{{annonce.title}}</h2>
            </div>
            <div class="card-body">
                <p>{{annonce.content}}</p>
            </div>
        </div>
    </div>
{% endblock %}